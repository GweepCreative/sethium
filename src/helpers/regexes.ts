export const JobNameRegex = /^[a-zA-Z0-9_\sığüşöçİĞÜŞÖÇ]{3,32}$/;
export const WalletAdressRegex = /^ID[0-9]{1,20}$/;
export const EmojiRegex =  /^<a?:[a-zA-Z0-9_]+:[0-9]{17,}>$/;

export const ButtonWithFishInFishing = /^fishing\.\d{16,}\.[a-zA-Z0-9_\sığüşöçİĞÜŞÖÇ]{3,32}\.[0-9]{13}$/;

export const RecipeRegex = /^(\d*\*\d*\+)*\d*\*\d*$/

export const SafeContentTitle = /^[a-zA-Z0-9_\sığüşöçİĞÜŞÖÇ]{3,32}$/;

export const variantRegex = /^[a-z0-9_]{3,32}$/;

export const upgradesRegex = /^(\d+\s*,\s*)*\d+$/;

export const LatinRegex = /^[A-zÀ-ÿ\s'\.,-\/#!$%\^&\*;:{}=\-_`~()\dğüşöçıİĞÜŞÖÇ]{0,32}$/;